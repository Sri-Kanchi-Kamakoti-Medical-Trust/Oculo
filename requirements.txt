# Oculo benchmark - Python dependencies
# Tested with Python 3.10-3.13. For GPU training install a CUDA build of
# torch/torchvision from https://pytorch.org (the pins below are CPU-agnostic).

torch>=2.2            # tested: 2.12.0
torchvision>=0.17     # tested: 0.27.0
timm==1.0.27
numpy>=1.26
pandas==2.3.1
scikit-learn==1.7.1
opencv-python==4.13.0.92
matplotlib==3.10.5
Pillow==11.3.0
tqdm==4.67.3
python-dotenv>=1.0
huggingface_hub>=0.24   # optional: download the released dataset

# Foundation-model extras (only needed for the OpenUS runs) -- see
# docs/foundation_models.md:
#   mamba-ssm, einops, fvcore
